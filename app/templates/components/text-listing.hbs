<div class="text-listing">
  {{#if @text.imgSrc}}
    <img src={{@text.imgSrc}} alt={{@text.name}} class="md-avatar">
  {{else}}
    {{svg-jar "book"}}
  {{/if}}
  <div class="md-list-item-text">
    <h3>{{#link-to "workbench.texts.text" @text.slug}}{{text-title @text}}{{/link-to}}</h3>
    <h4>{{@text.slug}}</h4>
  </div>
</div>

