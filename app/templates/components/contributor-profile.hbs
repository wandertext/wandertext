<h2>{{@model.firstName}} {{@model.lastName}}</h2>
<h3><a href="http://github.com/{{@model.username}}">@{{@model.username}}</a></h3>

<div>
  <div>
    <div>Texts {{svg-jar "arrow_down"}}</div>
    <div>
      Texts {{svg-jar "arrow_up"}}
      <TextList @texts={{@model.texts}} />
    </div>
  </div>
  <div>
    <div>Texts {{svg-jar "arrow_down"}}</div>
  </div>
</div>
