<ul class="list {{if (eq this.router.currentRouteName "texts.text") "transform -translate-x-full md:translate-x-0 md:w-1/4 w-full" "w-full"}}">
  {{#each @model as |item|}}
    <ListItem @model={{item}} @linkToRoute={{@linkToRoute}} />
  {{/each}}
</ul>
