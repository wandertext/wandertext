<div class="h-16 p-2 pr-6 max-w-full w-full flex flex-row space-x-2 justify-between">
  {{#if @linkToRoute}}
    <LinkTo class="overflow-hidden" @route={{@linkToRoute}} @model={{@model.id}}>
      <InfoBox::Text @model={{@model}}/>
    </LinkTo>
  {{else}}
    <InfoBox::Text @model={{@model}}/>
  {{/if}}
    {{yield}}
</div>
