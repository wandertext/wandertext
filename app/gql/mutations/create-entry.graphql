mutation createEntry($contributor: ID!, $entry: Entry) {
  createEntry(contributor: $contributor, entry: $entry) {
    contributor
    success
    message
    entry {
      id
      attestedName
      properties
    }
  }
}
    
