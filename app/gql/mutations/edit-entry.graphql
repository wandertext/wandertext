mutation editEntry($entryJSON: JSON!, $contributorId: ID!) {
  editEntry(contributorId: $contributorId, entryJSON: $entryJSON) {
    success
    message
    newEntry
    entry {
      id
      attestedName
      properties
      modifiedAt
    }
  }
}
    
