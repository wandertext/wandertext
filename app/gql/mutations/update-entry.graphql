mutation updateEntry($contributor: ID!, $id: ID!, $attestedName: String, $properties: JSON) {
  updateEntry(contributor: $contributor, id: $id, attestedName: $attestedName, properties: $properties) {
    contributor
    success
    message
    entry {
      id
      attestedName
      properties
    }
  }
}
    
