query text($id: ID!) {
  text(id: $id) {
    id
    name
    markdownName
    markdownBlurb
    entrySort
    entryProperties {
      name
      type
      readOnly
      inputLabel
      owner
    } 
    sortedEntries {
      id
      attestedName
      properties
    }
  }
}

