query text($id: ID!) {
  text(id: $id) {
    id
    name
    imgSrc
    markdownName
    markdownBlurb
    entryCount
    entryProperties {
      name
      type
      help
      readOnly
      inputLabel
      owner
      nullable
    }
  }
}

