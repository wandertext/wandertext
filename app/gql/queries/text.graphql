query text($id: ID!) {
  text(id: $id) {
    id
    name
    markdownName
    markdownBlurb
    entryProperties {
      name
      type
      help
      readOnly
      inputLabel
      owner
      nullable
    }
  }
}

